import React from 'react';

export default function noData(props) {
  return (
    <div>
      NoData
    </div>
  );
}

noData.propTypes = {
 
};

noData.defaultProps = {

};

noData.displayName = 'NoData';
