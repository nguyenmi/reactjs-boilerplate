const initialState = {
  loadingApp: false
};
// Gọi API get thông báo ở sidebar văn bản đến
export default function common(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
